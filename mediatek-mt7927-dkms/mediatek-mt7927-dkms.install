_pkgname=mediatek-mt7927
_pkgver=2.1

post_install() {
  dkms add "${_pkgname}/${_pkgver}"
  dkms build "${_pkgname}/${_pkgver}"
  dkms install "${_pkgname}/${_pkgver}"
}

pre_upgrade() {
  pre_remove
}

post_upgrade() {
  post_install
}

pre_remove() {
  dkms remove "${_pkgname}/${_pkgver}" --all || true
}
